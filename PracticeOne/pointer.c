#include <stdio.h>

int main() {

    int value = 10; // variable

    // printing variables and its memory address
    printf("Value: %d, Address: %x\n", value, &value);

    int *y;

    return 0;
}